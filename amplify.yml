version: 1
frontend:
  phases:
    prebuild:
        - npm install rimraf --global
        - npm install rimraf --save
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
