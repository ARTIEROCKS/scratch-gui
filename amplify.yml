version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install rimraf --global
        - npm install mkdirp --global
        - npm install webpack --global
    build:
      commands:
        - npm run clean
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
