version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install rimraf --global
        - npm install mkdirp --global
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
