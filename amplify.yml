version: 1
frontend:
  phases:
    prebuild:
      commands:
        - npm install rimraf --global
        - npm install rimraf --save
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
